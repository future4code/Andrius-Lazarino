

const operation:string = String(process.argv[2]).toUpperCase()

switch (operation){
    case "CREATESTUDENT":{
        const name:string = String(process.argv[3]).toUpperCase()
        const email:string = String(process.argv[4]).toUpperCase()
        const specialty:string = String(process.argv[5]).toUpperCase()
    }
}